#!/usr/bin/env bash
# This script lists files in the current directory
# and displays only the part after the first dash using a for loop
for file in *
do
    # skip hidden files automatically because '*' does not match dot-files
    echo "${file#*-}"
done
